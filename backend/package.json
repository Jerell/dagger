{
  "name": "dagger-backend",
  "version": "0.1.0",
  "type": "module",
  "description": "Hono API server for Dagger network inspection",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "build:wasm": "cd ../cli && wasm-pack build --target web --out-dir ../backend/pkg",
    "build:wasm:release": "cd ../cli && wasm-pack build --release --target web --out-dir ../backend/pkg"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hono/node-server": "^1.12.0",
    "hono": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}

