# dagger

working with file based directed graphs

I intend to use toml files for branches, directories for branch groups,
and have componenets defined within the branch toml files.

I also want to have global, group, branch, and component scoped variables.
